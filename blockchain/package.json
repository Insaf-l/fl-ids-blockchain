{
  "name": "blockchain",
  "version": "1.0.0",
  "private": true,
  "description": "Smart contract Hardhat pour le projet Federated Learning blockchain.",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "hardhat": "^2.28.6"
  }
}
